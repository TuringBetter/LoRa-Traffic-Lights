/**
  ******************************************************************************
  * @file    LoRaHandler.h
  * @author  陕西交通电子工程科技有限公司
  * @version V1.0.0
  * @date    2025-07-19
  * @brief   LoRa通信应用层处理器，负责解析和分发下行指令。
  * @details
  * > 模块职责:
  * 本模块作为LoRa通信的应用层，接收由`LoRaModule`传入的已解析的下行数据
  * (端口号和负载)，并根据预设的规则将其分发给相应的业务模块。
  *
  * > 设计模式:
  * 采用函数指针数组`portHandlers`作为指令分发器。LoRaWAN消息的端口号(port)
  * 被用作数组索引，直接调用对应的处理函数，实现了高效、解耦和易于扩展的
  * 指令处理架构。
  *
  * > 主要功能:
  * - 解析具体指令，如设置LED颜色、亮度、频率、开关等。
  * - 处理多播组加入指令，并将密钥信息存入NVS。
  * - 响应延迟测量和时间同步请求。
  *
  ******************************************************************************
  */
 
 #pragma once
#include <Arduino.h>

void handlePayload(uint8_t port, const String& payload);

uint32_t getRealTimeMs();